# Core dependencies
# No external dependencies required for basic functionality

# Local development dependencies
unctools>=0.1.0  # For enhanced UNC path support - required for local development

# Optional dependencies for end users
# unctools>=0.1.0  # For enhanced UNC path support (install with pip install .[unc])

# Development dependencies (install with pip install .[dev])
# pytest>=7.0.0
# black>=23.0.0
# flake8>=6.0.0
# mypy>=1.0.0